[slack]
url = "{{ notify_slack_url }}"
token = "{{ notify_slack_token }}"
channel = "{{ notify_slack_channel }}"
username = "{{ notify_slack_username }}"
icon_emoji = "{{ notify_slack_icon_emoji }}"
interval = "{{ notify_slack_interval }}"
