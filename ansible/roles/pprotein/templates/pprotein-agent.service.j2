[Unit]
Description=pprotein-agent service

[Service]
ExecStart=/usr/local/bin/pprotein-agent
WorkingDirectory=/opt/pprotein
Environment=PATH=$PATH:/usr/bin:/usr/local/bin
Environment=PPROTEIN_GIT_REPOSITORY={{ web_dir }}
Restart=always
User=root

[Install]
WantedBy=multi-user.target
